### 数组乱序
---

```
const shuffleArray = (arr) => arr.sort(() => Math.random() - 0.5)

let arr = ['a', 'b', 'c', 'd', 'e', 'f', 'g']

console.log(shuffleArray(arr))

// ["f", "a", "g", "d", "e", "b", "c"]
```